# いろは — AI配色 & タイポジェネレーター 仕様書（現状）

> **現状コード起こし仕様書 — 2026-06-26**
> spec_0622.md からの変更点を **★差分** として各所に記載。

---

## 0. プロジェクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | いろは |
| 目的 | AI（Gemini）がユーザーの指示から「配色5色＋フォントペア」を**2案**提案するジェネレーター |
| デザイン | ハイブリッド：操作系＝ニューモーフィズム（アクセント＝ティール `#1aa39a`）／結果・プレビュー＝白地 |
| フェーズ | Phase 1（本ドキュメント）/ Phase 2（ロードマップ参照） |
| 構成 | 単一アプリ（`index.html` + `style.css` + `js/` ディレクトリ） |
| ビルドツール | なし（Babel CDN で JSX をブラウザ変換） |

---

## 1. ファイル構成（★spec_0622 から変更）

```
iroha_generator/
│
├── .gitignore                  # config.js を除外
│
├── index.html
├── style.css
│
└── js/
    ├── config.js               # ★ APIキー管理（Git管理外）
    ├── config.example.js       # キー空の雛形（Git管理）
    ├── font-list.js            # ★ window.FONT_LIST（旧 data/font-list.js から移動）
    ├── logic.js                # ★ NEW：純粋JSロジック（JSXなし）
    └── app.js                  # React コンポーネント（JSXのみ）
```

> **★変更点**
> - `data/font-list.js` → `js/font-list.js` に移動。
> - ロジックを `logic.js` に分離。`app.js` は React コンポーネントのみ。
> - `index.html` 開発時は `js/config.example.js` を直接読み込む（`config.js` が存在しない環境向け）。

### スクリプト読み込み順（index.html）

```html
<!-- 1. Google Fonts（表示用） -->
<link href="https://fonts.googleapis.com/css2?...（プリロード10書体）..." rel="stylesheet">

<!-- 2. 自分のCSS -->
<link rel="stylesheet" href="style.css">

<!-- 3. React / Babel CDN -->
<script src="https://unpkg.com/react@18.3.1/umd/react.development.js"></script>
<script src="https://unpkg.com/react-dom@18.3.1/umd/react-dom.development.js"></script>
<script src="https://unpkg.com/@babel/standalone@7.29.0/babel.min.js"></script>

<!-- 4. APIキー（config.js or config.example.js） -->
<script src="js/config.example.js"></script>

<!-- 5. フォントデータ -->
<script src="js/font-list.js"></script>

<!-- 6. ロジック（JSXなし・通常script） -->
<script src="js/logic.js"></script>

<!-- 7. UIコンポーネント（JSX） -->
<script type="text/babel" src="js/app.js"></script>
```

> `app.js` は `window.IROHA_CONFIG`・`window.FONT_LIST`・`logic.js` のグローバル関数に依存。

---

## 2. config.js（APIキー管理）

```js
// js/config.js（Git管理外）
window.IROHA_CONFIG = {
  GEMINI_API_KEY: "AIza...ここにキーを貼る",
  MODE:      "direct",  // "direct"（課題提出版） | "proxy"（公開版）
  MODEL:     "gemini-3.1-flash-lite",
  PROXY_URL: "/api/generate.php",
};
```

### config.example.js（Git管理する雛形）

```js
window.IROHA_CONFIG = {
  GEMINI_API_KEY: "",
  MODE:      "direct",
  MODEL:     "gemini-3.1-flash-lite",
  PROXY_URL: "/api/generate.php",
};
```

> `config.example.js` はキーが空のまま Git に含める。実際に使う `config.js` は `.gitignore` で除外する。

---

## 3. .gitignore

```
# APIキー管理ファイル（コミットしない）
config.js
js/config.js

.DS_Store
Thumbs.db
.vscode/
.idea/
```

---

## 4. デザインシステム（style.css）

**方針**：操作系（サイドバー）＝ニューモーフィズム／結果・プレビュー＝白地。アクセントのティールは操作系のみ。

```css
:root {
  /* 操作系の背景 */
  --neu-bg:   #e8ebf1;
  --neu-bg-3: #e2e6ee;

  /* 結果・プレビューの背景 */
  --surface:        #ffffff;
  --surface-2:      #f7f8fa;
  --surface-border: rgba(20,24,31,.08);

  /* テキスト */
  --text-strong:    #20242f;
  --text-body:      #3a4150;
  --text-secondary: #5a6271;
  --text-muted:     #8a93a5;
  --ink-900:        #20242f;

  /* アクセント（操作系のみ） */
  --teal-600: #1aa39a;
  --teal-500: #28b8ad;
  --teal-700: #15847d;

  --color-border: rgba(140,150,170,.22);

  /* フォント */
  --font-display: "Poppins","Zen Kaku Gothic New",sans-serif;
  --font-body:    "Zen Kaku Gothic New","Noto Sans JP",sans-serif;
  --font-mono:    "Space Mono",monospace;

  /* ニューモーフィズムシャドウ */
  --shadow-neu-xs:      2px 2px 5px rgba(166,174,190,.5), -2px -2px 5px rgba(255,255,255,.7);
  --shadow-neu-sm:      4px 4px 9px rgba(166,174,190,.55), -4px -4px 9px rgba(255,255,255,.75);
  --shadow-neu-md:      6px 6px 14px rgba(166,174,190,.6), -5px -5px 12px rgba(255,255,255,.8);
  --shadow-neu-inset:   inset 3px 3px 7px rgba(166,174,190,.55), inset -3px -3px 7px rgba(255,255,255,.7);
  --shadow-neu-pressed: inset 4px 4px 9px rgba(166,174,190,.6), inset -4px -4px 9px rgba(255,255,255,.7);

  /* 生成ボタン */
  --shadow-teal-sm: 3px 3px 9px rgba(26,163,154,.35), -2px -2px 6px rgba(255,255,255,.6);

  /* 白地カードの影 */
  --shadow-soft-sm: 0 1px 3px rgba(20,24,31,.06), 0 1px 2px rgba(20,24,31,.04);
  --shadow-soft-md: 0 4px 12px rgba(20,24,31,.08), 0 2px 4px rgba(20,24,31,.04);

  /* 生成結果の動的変数（applyToCSS で上書き） */
  --gen-primary:   #1aa39a;
  --gen-secondary: #5a6271;
  --gen-accent:    #28b8ad;
  --gen-base:      #ffffff;
  --gen-text:      #20242f;
}
```

> **★spec_0622 との差分**：`--shadow-neu-inset-sm` は現在の CSS に存在しない（削除済み）。

---

## 5. 全体レイアウト

```
[スティッキーヘッダ 64px]
  ロゴ「いろは」 ＋ ロゴドット（--gen-primary でリアルタイム変色）
  タグライン（Space Mono）

[.page（max-width 1300px / flex row）]
  [.sidebar 320px / スティッキー top:64px / ニューモーフィズム]
    .neu-card（入力UI）
    .fav-summary（お気に入り件数）

  [.content flex:1 / 白地]
    .content-tabs（タブ：提案 / お気に入り）
    .content-body（タブ内容）

[.toast（fixed bottom center）]
```

ブレークポイント `max-width: 900px` でサイドバーが上に移動・1カラム化。

---

## 6. 入力UI（サイドバー・js/logic.js の定数・app.js の UI）

### 6-1. メインカラー（MAIN_COLORS）

```js
// js/logic.js
const MAIN_COLORS = [
  { key: "red",      label: "赤",       hint: "#d8453c" },
  { key: "orange",   label: "オレンジ", hint: "#e8843c" },
  { key: "yellow",   label: "黄",       hint: "#e8c23c" },
  { key: "green",    label: "緑",       hint: "#4ca85f" },
  { key: "blue",     label: "青",       hint: "#3c6fe8" },
  { key: "purple",   label: "紫",       hint: "#8a5ce8" },
  { key: "monotone", label: "モノクロ", hint: "#5a6271" },  // ★"モノトーン"→"モノクロ"
];
```

- 単一選択。もう一度押すと解除（おまかせ）。
- `hint` はボタン上の小さな色見本にのみ使う（生成色ではない）。

### 6-2. トンマナ（TONE_AXES）

```js
// js/logic.js
const TONE_AXES = [
  { key: "form",   left: "やわらか",   right: "かっちり" },
  { key: "era",    left: "クラシック", right: "モダン" },
  { key: "energy", left: "にぎやか",   right: "静か" },
  { key: "class",  left: "カジュアル", right: "ラグジュアリー" },
];
```

- 各軸の値：`-1`（左）/ `0`（中央）/ `1`（右）。初期値：全軸 `0`。
- ToneSlider コンポーネント：クリック位置を3分割して `-1` / `0` / `1` にスナップ。
- **中央（0）の軸はプロンプトに含めない**。

### 6-3. 用途トグル（"digital" | "print"）

初期値 `"digital"`。

### 6-4. フォント分け（"split" | "single"）

初期値 `"split"`（タイトルと本文で別フォントを提案）。

### 6-5. 日英の組み合わせ（"bilingual" | "jp_only"）

初期値 `"bilingual"`（日本語＋英語フォントの組み合わせ）。

### 6-6. 自由記入

`textarea`、placeholder：`例：老舗の和菓子屋のチラシを作りたい`。自由記入は最優先。

---

## 7. logic.js — ロジック関数一覧（★spec_0622 で app.js にあったものを分離）

`js/logic.js` は JSX なしの通常 `<script>` で読み込む。以下のグローバル変数・関数を公開する。

| 識別子 | 種別 | 内容 |
|--------|------|------|
| `MAIN_COLORS` | const | メインカラー選択肢（7件） |
| `TONE_AXES` | const | トンマナ軸定義（4軸） |
| `ROLE_LABELS` | const | `{primary:"Primary", ...}` |
| `C` | object | 色ユーティリティ（§14参照） |
| `loadFont` | function | Google Fonts 動的ロード |
| `guardPalette` | function | Gemini出力の検証・補正 |
| `resolveFont` | function | フォント検証・最近傍weight |
| `applyToCSS` | function | CSS変数動的適用 |
| `copyText` | function | クリップボードコピー |
| `mainColorLabel` | function | key → 日本語ラベル |
| `buildPrompt` | function | ユーザー指示プロンプト組み立て |
| `buildSystemPrompt` | function | システムプロンプト生成 |
| `RESPONSE_SCHEMA` | const | Gemini responseSchema定義 |
| `callGemini` | function | Gemini API 呼び出し |
| `generate` | function | 生成フロー全体 |
| `buildDesignPrompt` | function | 定型文プロンプト生成 |
| `genInitial` / `genReducer` | const/function | 生成 useReducer |
| `favInitial` / `favReducer` | const/function | お気に入り useReducer |

---

## 8. Gemini API 呼び出し（callGemini / generate）

### 8-1. callGemini

```js
async function callGemini(bodyPayload) {
  if (CONFIG.MODE === "proxy") {
    // PHP プロキシ経由
    const res = await fetch(CONFIG.PROXY_URL, { method:"POST", ... });
    ...
  } else {
    // ブラウザ直叩き
    const url = `https://generativelanguage.googleapis.com/v1beta/models/${CONFIG.MODEL}:generateContent?key=${encodeURIComponent(GEMINI_API_KEY)}`;
    ...
  }
}
```

### 8-2. プロンプト組み立て（buildPrompt）

```js
function buildPrompt({mainColor, tone, purpose, fontMode, langMode, free}) {
  const parts = [];
  if (mainColor) parts.push(`メインカラーの方向性：${mainColorLabel(mainColor)}`);
  // tone：中央(0)の軸は含めない
  const toneParts = [];
  TONE_AXES.forEach(ax=>{
    if(tone[ax.key]===-1) toneParts.push(`${ax.left}寄り`);
    if(tone[ax.key]===1)  toneParts.push(`${ax.right}寄り`);
  });
  if (toneParts.length) parts.push(`トンマナ：${toneParts.join("、")}`);
  parts.push(purpose==="print" ? "用途：印刷物" : "用途：デジタル（Web）");
  parts.push(fontMode==="single" ? "フォント：タイトルと本文で同一..." : "フォント：タイトルと本文で別...");
  parts.push(langMode==="jp_only" ? "フォント言語：日本語フォントのみ" : "フォント言語：日本語フォントと英語フォントの組み合わせ");
  if (free?.trim()) parts.push(`自由記入（最優先で反映）：${free.trim()}`);
  if (!parts.length) parts.push("おまかせで、汎用的に使いやすい上質な配色とフォントを提案してください。");
  return parts.join("\n");
}
```

### 8-3. システムプロンプト（buildSystemPrompt）★spec_0622から追加・変更あり

```js
function buildSystemPrompt(fontCatalog, purpose) {
  return [
    "あなたは日本語UI向けのアートディレクターです。...",
    // 【フォント】セクションに以下を追加：
    "bilingual（日英の組み合わせ）の場合は、heading に subset が latin のフォントを、body に subset が jp のフォントを選んでください。",
    `カタログ: ${JSON.stringify(fontCatalog)}`,
    // 【印刷用途】は purpose==="print" のときのみ含める
    purpose==="print" ? "【印刷用途】..." : "",
    ...
  ].filter(Boolean).join("\n");
}
```

**fontCatalog は `family` / `subset` / `category` の3フィールドのみ**（weights は省く）。

### 8-4. responseSchema（構造化JSON強制）

spec_0622 § 7-3 と同一。2案配列、各案に `theme / reason / fontReason / palette[5] / fontPairing{heading,body}` を要求。

### 8-5. generationConfig

```json
{
  "responseMimeType": "application/json",
  "responseSchema": {...},
  "temperature": 0.9,
  "maxOutputTokens": 900
}
```

### 8-6. generate（生成フロー）

```js
async function generate(input) {
  // 1. プロンプト組み立て
  const body = {
    system_instruction: { parts: [{ text: buildSystemPrompt(fontCatalog, input.purpose) }] },
    contents: [{ role:"user", parts: [{ text: buildPrompt(input) }] }],
    generationConfig: { ... },
  };

  // 2. API呼び出し（失敗時1回リトライ）
  let data;
  try { data = await callGemini(body); }
  catch(e) { data = await callGemini(body); }  // 1回リトライ

  // 3. JSON パース
  const raw = data?.candidates?.[0]?.content?.parts?.[0]?.text || "";
  const parsed = JSON.parse(raw.replace(/```json|```/g,"").trim());

  // 4. 各案を検証・補正
  return parsed.proposals.map(p => {
    const palette = guardPalette(p.palette);
    const needJp = input.langMode === "jp_only";
    const {font: hFont, weight: hW} = resolveFont(p.fontPairing.heading.family, p.fontPairing.heading.weight, needJp);
    let {font: bFont, weight: bW} = resolveFont(p.fontPairing.body.family, p.fontPairing.body.weight, needJp);
    if (input.fontMode === "single") { bFont = hFont; bW = hW; }
    loadFont(hFont.family, hFont.weights);
    if (bFont.family !== hFont.family) loadFont(bFont.family, bFont.weights);
    return { ...p, palette, fontPairing: { heading: {...}, body: {...} } };
  });
}
```

---

## 9. 検証ガード

### 9-1. guardPalette

1. 5 role（primary/secondary/accent/base/text）が揃っているか確認。不足は `#888888` で補完。
2. `hex` が `/^#[0-9A-Fa-f]{6}$/` でない場合も `#888888` に置換。
3. `base` と `text` のコントラスト比が 4.5 未満 → `text.hex` を補正（base が明るければ `L=18%`、暗ければ `L=92%`）。

### 9-2. resolveFont

1. `family` が `FONT_LIST` に存在するか大文字小文字無視で確認。
2. 不在 → `preferJp=true` なら `subset="jp"` の先頭、それ以外はリスト先頭。
3. `weight` が該当フォントの `weights` に存在しない → 最近傍に丸める。
4. `fontMode === "single"` のとき `generate` 側で `bFont = hFont` に統一。

---

## 10. FONT_LIST（js/font-list.js）★spec_0622 から大幅拡張

`window.FONT_LIST` として公開。前回 font-selector の成果物を反映した大型リスト。

**★変更点**：spec_0622 の15フォントから約130フォント以上に拡張。

```js
window.FONT_LIST = [
  // 和文（subset: "jp"）約65フォント
  { family: "Noto Sans JP",     category: "sans-serif",  subset: "jp", weights: [100,200,300,400,500,600,700,800,900] },
  { family: "Noto Serif JP",    category: "serif",       subset: "jp", weights: [200,300,400,500,600,700,800,900] },
  { family: "M PLUS Rounded 1c",category: "sans-serif",  subset: "jp", weights: [100,300,400,500,700,800,900] },
  { family: "LINE Seed JP",     category: "sans-serif",  subset: "jp", weights: [100,400,700,800] },
  { family: "Dela Gothic One",  category: "display",     subset: "jp", weights: [400] },
  // ...（65フォント）

  // 欧文（subset: "latin"）約65フォント
  { family: "Roboto",           category: "sans-serif",  subset: "latin", weights: [100,200,300,400,500,600,700,800,900] },
  { family: "Playfair Display", category: "serif",       subset: "latin", weights: [400,500,600,700,800,900] },
  { family: "Space Mono",       category: "monospace",   subset: "latin", weights: [400,700] },
  // ...（65フォント）
];
```

カテゴリ種別：`sans-serif` / `serif` / `display` / `handwriting` / `monospace`

---

## 11. 結果表示（app.js）

### 11-1. 2案カード一覧（ProposalList相当）

```
┌─────────────────────┐  ┌─────────────────────┐
│ A案                 │  │ B案                 │
│ {theme}             │  │ {theme}             │
│ ██ ██ ██ ██ ██      │  │ ██ ██ ██ ██ ██      │  ← 5色バー(36px)
│ 見出し {family}     │  │ 見出し {family}     │
│ 本文　 {family}     │  │ 本文　 {family}     │
│ [この案を見る →]    │  │ [この案を見る →]    │
└─────────────────────┘  └─────────────────────┘
```

クリックで詳細モードへ（`genDispatch({type:"SELECT",index:i})`）。

### 11-2. 詳細モード

```
[← 2案にもどる]

[コンセプト帯]
  ■(primary 44px角) Concept / {theme} / {reason}

[カラーパレット]  ← detail-section
  5色チップ（role / 日本語名 / HEX）+ クリックでHEX・RGBコピー
  [📋 5色コピー] [⚙️ CSS変数] [🖨 CMYK(参考)（印刷時のみ）]  [☆ お気に入り]

[フォントペア]  ← detail-section
  💬 {fontReason}
  [見出しカード] [本文カード]（fontMode=single のとき1枚）
    各カード：role / フォント名pill / プレビュー / category·subset
  [📋 フォント名コピー] [⚙️ CSS変数] [☆ お気に入り]

[プレビュー]  ← detail-section
  ラベル（primary色のドット＋用途ラベル）
  用途 digital → SitePreview / print → FlyerPreview
```

### 11-3. カラーコピー形式

| ボタン | コピー内容 |
|--------|-----------|
| 5色コピー | `#HEX / rgb(r,g,b)` をHEX単体で並べたカンマ区切り文字列 |
| CSS変数 | `:root { --color-primary: #HEX; ... }` |
| CMYK(参考) | `Primary: C0 M85 Y82 K15\n...（参考値）` |

> 各チップクリックは `{name} ({hex})` をコピーし、トースト表示。

---

## 12. プレビューコンポーネント（app.js）

```jsx
<Preview palette={palette} fonts={fonts} purpose={purpose} />
// → purpose==="print" ? <FlyerPreview> : <SitePreview>
```

### 12-1. SitePreview（Webサイト風）

| 要素 | 使用role | フォント |
|------|---------|---------|
| ナビロゴ | primary色 | fonts.heading |
| ナビリンク | text色（opacity .7） | fonts.body |
| アクセントバー（3px横線） | accent | — |
| eyebrow pill | primary（背景18%透過） | fonts.body |
| 大見出し | text | fonts.heading |
| リード文 | secondary | fonts.body |
| CTAボタン | primary地・textOn(primary)文字 | fonts.heading |
| サブリンク | accent（opacity .7） | fonts.body |

### 12-2. FlyerPreview（チラシ風）

| 要素 | 使用role | フォント |
|------|---------|---------|
| ヘッダ帯 | primary地 | — |
| ヘッダラベル | textOn(primary) | fonts.body |
| 大タイトル | textOn(primary) | fonts.heading |
| 本文テキスト | text | fonts.body |
| アクセントライン | accent（背景18%透過） | — |
| サブ情報 | secondary | fonts.body |
| フッター | secondary（opacity .6） | fonts.body |

---

## 13. お気に入り（ワークスペース）

### 13-1. 保存データ構造（localStorage）

```js
// "iroha-fav-colors"
[
  { id: "テーマ名-0", theme: "朱赤の和", palette: [{hex,role,name} × 5] },
  ...  // 最大10件（favReducer で slice(0,10)）★
]

// "iroha-fav-fonts"
[
  {
    id: "フォント名-0",
    fontReason: "...",
    heading: { family, weight, subset, category },
    body:    { family, weight, subset, category },
  },
  ...  // 最大10件★
]
```

> **★spec_0622 との差分**：各リストの上限は10件（`slice(0,10)`）。
> id は `{theme}-{selectedIndex}` または `{heading.family}-{selectedIndex}` の文字列。

### 13-2. ワークスペースUI（FavWorkspace）

- 配色リスト×フォントリストを2カラム表示（「×」マーカーで分離）。
- 各リストで1件ずつ選択中（ラジオ的）。
- 両方選択中 → Preview表示 + 定型文プロンプト表示・コピー。
- 片方のみ → 「配色とフォントを1つずつ選択するとプレビューが表示されます」。
- 両方未登録 → 空状態（☆マーク＋案内テキスト）。
- 削除：各アイテムの ✕ ボタン。削除後、選択は次の先頭に自動移動。

### 13-3. タブの件数表示（★spec_0622 には記載なし）

```jsx
お気に入り {(colors.length + fonts.length) > 0 ? `(${colors.length + fonts.length})` : ""}
```

---

## 14. 定型文プロンプト生成（buildDesignPrompt）★spec_0622 から変更

```js
function buildDesignPrompt(color, font, purpose) {
  const roleJa = {primary:"メイン",secondary:"サブ",accent:"アクセント",base:"背景",text:"文字"};
  const hexes = color.palette.map(p=>`${roleJa[p.role]}=${p.hex}`).join(", ");
  const kind = purpose==="print" ? "チラシ／印刷物" : "Webサイト";
  return [
    `次の配色とフォントで${kind}のデザイン案を作成してください。`,
    `配色: ${hexes}`,
    `見出しフォント（${font.heading.subset==="jp"?"和文":"欧文"}）: ${font.heading.family} (${font.heading.weight})`,
    `本文フォント（${font.body.subset==="jp"?"和文":"欧文"}）: ${font.body.family} (${font.body.weight})`,
    `メインカラーを主役、アクセントをポイント、背景色を地色、文字色をテキストに使ってください。`,
  ].join("\n");
}
```

> **★変更点**：role ラベルを日本語化（`roleJa`）。フォント名の後ろに `(和文)` / `(欧文)` を追加。

---

## 15. 色ユーティリティ（C オブジェクト・js/logic.js）

```js
const C = {
  hexToRGB(hex)       // → { r, g, b }
  hexToHSL(hex)       // → { h, s, l }  （h:0-360, s:0-100, l:0-100）
  hslToHex(h, s, l)   // → "#rrggbb"
  rgbToCMYK(r, g, b)  // → { c, m, y, k }  単純変換・参考値
  lum(hex)            // 相対輝度（WCAG）
  contrast(a, b)      // コントラスト比
  textOn(bg)          // lum > 0.4 → '#20242f'、それ以外 → '#ffffff'
  valid(h)            // /^#[0-9A-Fa-f]{6}$/ テスト
}
```

---

## 16. CSS変数への動的適用 / トースト

### 16-1. applyToCSS

```js
function applyToCSS(palette) {
  const map = {primary:"--gen-primary", secondary:"--gen-secondary",
               accent:"--gen-accent", base:"--gen-base", text:"--gen-text"};
  palette.forEach(p=>document.documentElement.style.setProperty(map[p.role], p.hex));
}
```

ロゴドット（`.logo-dot`）は `background: var(--gen-primary)` で自動反映。

### 16-2. トースト（useToast フック）

- `showToast(msg)` で表示、2000ms後に `.out` でフェードアウト。
- 連続呼び出し時はタイマーをリセット。

---

## 17. スケルトンローディング（Skeleton コンポーネント）

生成中（`status === "loading"`）に2案分のスケルトンカードを表示。

```
[sk-line 60%]
[sk-line 85%]
[skeleton-color × 5]  ← 5色分
[sk-line 70%]
[sk-line 50%]
```

---

## 18. 状態管理

### 18-1. 入力フォーム（useState）

```js
const [mainColor, setMainColor] = useState(null);          // null=おまかせ
const [tone, setTone] = useState({form:0,era:0,energy:0,class:0});
const [purpose, setPurpose] = useState("digital");
const [fontMode, setFontMode] = useState("split");
const [langMode, setLangMode] = useState("bilingual");
const [free, setFree] = useState("");
const [activeTab, setActiveTab] = useState("proposal");
```

### 18-2. 生成（useReducer）

```js
// genInitial: { status:"idle", proposals:[], selectedIndex:null, error:null }
// status: "idle" | "loading" | "ready" | "error"
// actions:
//   GENERATE_START   → status:"loading", error:null, selectedIndex:null
//   GENERATE_SUCCESS → status:"ready", proposals
//   GENERATE_ERROR   → status:"error", error
//   SELECT           → selectedIndex（null で一覧に戻る）
//   RESET            → genInitial
```

### 18-3. お気に入り（useReducer）

```js
// favInitial: { colors:[], fonts:[], selColorId:null, selFontId:null }
// actions:
//   HYDRATE       → ローカルストレージから復元
//   ADD_COLOR     → 重複チェック後に先頭追加（最大10件）、selColorId を初期設定
//   REMOVE_COLOR  → 削除後、selColorId を次の先頭に
//   SELECT_COLOR  → selColorId 更新
//   ADD_FONT      → 同上（fonts）
//   REMOVE_FONT   → 同上
//   SELECT_FONT   → selFontId 更新
```

localStorage 同期：`useEffect` で `favState.colors` / `favState.fonts` 変更時に `setItem`。
初回マウント：`useEffect` で `getItem` → `HYDRATE`。

---

## 19. React コンポーネント構成（app.js）

```
App
├── <header>  ロゴ・ロゴドット・タグライン
├── <aside.sidebar>
│   ├── .neu-card
│   │   ├── メインカラーボタン（MAIN_COLORS × 7）
│   │   ├── ToneSlider × 4（TONE_AXES）
│   │   ├── 用途トグル
│   │   ├── フォント分けラジオ
│   │   ├── 日英ラジオ
│   │   ├── 自由記入 textarea
│   │   └── 生成ボタン
│   └── .fav-summary（配色N · フォントN）
│
└── <main.content>
    ├── .content-tabs  [提案] / [お気に入り(N)]
    └── .content-body
        ├── (提案タブ)
        │   ├── .err-banner（error時）
        │   ├── <Skeleton />（loading時）
        │   ├── .state-empty（idle時）
        │   ├── .proposals-list（ready & selectedIndex=null）
        │   └── 詳細モード（ready & selectedIndex!==null）
        │       ├── .detail-back-btn
        │       ├── .concept-card
        │       ├── .detail-section > <PaletteResult />
        │       ├── .detail-section > <FontResult />
        │       └── .detail-section > <Preview />
        │
        └── (お気に入りタブ)
            └── <FavWorkspace />
                ├── .fav-columns（配色リスト × フォントリスト）
                ├── <Preview />（両方選択時）
                └── .prompt-copy-area（両方選択時）

// 共通部品
<Preview palette fonts purpose />
  └── purpose==="print" ? <FlyerPreview> : <SitePreview>

<ToneSlider axis value onChange />
<PaletteResult palette purpose onFavAdd alreadyFav showToast />
<FontResult fonts fontMode onFavAdd alreadyFav showToast />
<Skeleton />
<FavWorkspace favState favDispatch purpose showToast />
```

---

## 20. localStorage まとめ

| キー | 型 | 内容 |
|------|----|------|
| `iroha-fav-colors` | JSON配列（最大10件） | お気に入り配色 |
| `iroha-fav-fonts`  | JSON配列（最大10件） | お気に入りフォント |

APIキーは `window.IROHA_CONFIG` から取得。localStorage に保存しない。

---

## 21. CDN 依存

```html
<script src="https://unpkg.com/react@18.3.1/umd/react.development.js"></script>
<script src="https://unpkg.com/react-dom@18.3.1/umd/react-dom.development.js"></script>
<script src="https://unpkg.com/@babel/standalone@7.29.0/babel.min.js"></script>
```

`app.js` は `<script type="text/babel">` で Babel がブラウザ変換する。

---

## 22. spec_0622 からの主な変更点サマリー

| 項目 | spec_0622 | spec_0626（現状） |
|------|-----------|-----------------|
| ファイル構成 | `data/font-list.js`、ロジックは app.js 内 | `js/font-list.js` に移動、ロジックは `js/logic.js` に分離 |
| font-list の規模 | 15フォント | 約130フォント（和文65 + 欧文65） |
| カテゴリ | sans-serif / serif / monospace | ＋ display / handwriting を追加 |
| "モノクロ" ラベル | "モノトーン" | "モノクロ" |
| bilingual指示 | システムプロンプトに記述なし | heading=latin、body=jp の明示指定を追加 |
| buildDesignPrompt | role英語ラベル、フォント名のみ | role日本語ラベル（roleJa）、subset情報（和文/欧文）追加 |
| お気に入り上限 | 記載なし | 各リスト最大10件 |
| お気に入りタブ件数 | 記載なし | タブに合計件数を表示 |
| --shadow-neu-inset-sm | CSS変数として定義 | 削除済み（CSSに存在しない） |

---

## 23. Phase 2 ロードマップ（実装対象外）

- お気に入り配色×フォントを入力に、バナー・サムネイルのプレビューを本格生成。
- フォトストック API 連携（Unsplash / Pexels）で画像を組み込む。
- 生成デザインを PNG ダウンロード。
- `buildDesignPrompt` を Gemini 整形版に差し替え。
- プレビューのモック → リアルレイアウト化。
